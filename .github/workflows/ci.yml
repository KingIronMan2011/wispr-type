name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Quality checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm
      - uses: dtolnay/rust-toolchain@stable
      - name: Install Linux system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            libclang-dev \
            file \
            libasound2-dev \
            libappindicator3-dev \
            librsvg2-dev \
            libssl-dev \
            libvulkan-dev \
            libwebkit2gtk-4.1-dev \
            libxdo-dev \
            patchelf \
            rpm \
            glslc
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: "src-tauri -> target"
          key: ubuntu-quality
      - run: pnpm install --frozen-lockfile
      - run: pnpm metadata:check
      - run: pnpm format:check
      - run: pnpm types
      - run: pnpm lint
      - run: pnpm test
      - run: pnpm test:rust
      - name: Compile the optional Vulkan Local Whisper backend
        run: cargo check --manifest-path src-tauri/Cargo.toml --features local-whisper-vulkan
      - run: pnpm exec playwright install --with-deps chromium
      - run: pnpm test:e2e
